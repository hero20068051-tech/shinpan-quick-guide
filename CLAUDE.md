# CLAUDE.md — shinpan-quick-guide 開発ルール

最初に必ず、

`docs/MASTER_BIBLE.md` をこのプロジェクトの基準資料として扱い、作業前に必要な範囲を確認すること。

Master Bible と指示が矛盾する可能性がある場合は、勝手に判断せずユーザーへ報告すること。

---

## 基本ルール

- ユーザーが指定した対象だけを変更する
- 「ついでに」別の場所を修正しない
- 問題を発見しても、指示対象外ならまず報告する
- 仕様が不明なら推測で実装しない
- 大きなリファクタリングを勝手に行わない
- 新機能を勝手に追加しない
- 既存機能を勝手に削除しない

---

## 競技規則カード

現在の `rules` 配列は 68 枚を維持する。

通常のカード編集では、

- 追加しない
- 削除しない
- 別カードと入れ替えない
- `title` を変更しない
- 似た名前から推測しない
- 必ず完全一致する `title` を確認する
- 1 枚ずつ編集する
- 前後カードを確認する
- 配列構造を壊さない

特に

- 「チャージ」
- 「ゴールキーパーへのチャージ」

など、似たカードを混同しない。

---

## カード編集後

必ず、

- `git diff`
- `rules` 配列が 68 枚か
- 対象カードだけが変更されたか
- 構文上の問題がないか

を確認する。

---

## 発見した既存問題について

先ほど確認したような、

- 死にコード
- HTML 構造の不整合
- 開発用コードの残存
- CSS class 名の不一致
- その他の既存問題

を発見しても、

「見つけたから直す」

という行動をしない。

まずユーザーへ報告する。

ユーザーが修正を決定した場合のみ変更する。

---

## Git

- カード編集は数枚ごとの適切な区切りで commit する。
- push 後は、可能な範囲で GitHub Actions と公開ページの状態を確認する。
- ただし、ユーザーが作業停止を指示した場合はそれに従う。
